import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    borderRadius: 999,
    overflow: "hidden",
    backgroundColor: "#f5e9da",
  },

  image: {
    width: "100%",
    height: "100%",
  },
});
